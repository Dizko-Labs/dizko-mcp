import assert from "node:assert/strict";import test from "node:test";import {cityFromInput,recordToolCall} from "../src/telemetry.js";
test("telemetry sends bounded facts only on trusted config",async()=>{let seen;await recordToolCall({tool:"search_events",city:"berlin",latency_ms:12,outcome:"success",result_count:3},{config:{apiBaseUrl:"https://api.test",upstreamSecret:"secret",apiTimeoutMs:1000},fetch:async(u,i)=>{seen={u,i};return new Response(null,{status:202})}});assert.equal(seen.i.headers["X-Dizko-MCP-Secret"],"secret");assert.deepEqual(JSON.parse(seen.i.body),{tool:"search_events",city:"berlin",latency_ms:12,outcome:"success",result_count:3});});
test("city normalization does not accept arbitrary input",()=>{assert.equal(cityFromInput({city:" New York "}),"new york");assert.equal(cityFromInput({city:{private:"x"}}),null);});
